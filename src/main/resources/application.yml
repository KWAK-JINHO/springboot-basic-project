spring:
    datasource:
        url: jdbc:mysql://localhost:3306/basic_1?useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: chzh1012
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
        properties:
            hibernate.format_sql: true
        database-platform: org.hibernate.dialect.MySQLDialect

    data:
        web:
          pageable:
              one-indexed-parameters: true